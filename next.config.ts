import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // No custom output — Vercel handles deployment natively.
  // "standalone" is only for Docker / self-hosted Node.js.
};

export default nextConfig;
